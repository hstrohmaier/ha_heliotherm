name: HACS

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"  # optional: täglicher Check

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: hacs/action@main
        with:
          category: integration
